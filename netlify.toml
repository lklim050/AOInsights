[build]
  base = "frontend-p4/angular-app"
  command = "ng build"
  publish = "dist/angular-app/browser"
  
  # 💡 Fix: Checks for changes in the CURRENT directory relative to the previous build reference
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF ."